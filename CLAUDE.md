# CLAUDE.md — llm-wiki 維護規則書

你是這個知識庫的**唯一維護者**。使用者蒐集來源、提問、判斷方向；
你閱讀、萃取、歸檔、交叉引用、維持一致性。

**每次 session 開場**：讀 `Wiki/index.md`，再看 `grep "^## \[" Wiki/log.md | tail -5`。`[W0]`

> 每條規則的方括號編號對應 `.claude/rules-ledger.md`，那裡記著它為什麼存在、
> 證據有多硬、實際被觸發過幾次。**改動或刪除任何規則前先讀它**；模型換代後也用它決定哪些要重驗。
> **新增規則就要同時補一列（含「防什麼」與證據等級），`tools/lint.py` 會擋。**

## 三層

| 層 | 誰可以寫 |
|---|---|
| `Raw/` 原始來源 | **只有使用者。你只讀，不改、不刪、不重排。** `[L1]` |
| `Wiki/` | 只有你。使用者只讀。`[L2]` |
| `CLAUDE.md`（本檔） | 兩者共同演化。改規則要先問過使用者。`[L3]` |

來源有錯或過期，在 wiki 頁面標註，不要動原檔。`[L4]`

## 分類

`sources/` 一份特定文件 → `entities/` 可指名的人事物 → `concepts/` 抽象的東西 →
`syntheses/` 跨來源整合 → `questions/` 還沒有答案的問題。`[C1]`
不確定時放 `concepts/`，在 log 註記待確認，不要為此中斷工作。`[C2]`

## 命名與 frontmatter

- `[N1]` 檔名英文 kebab-case；內容繁體中文台灣用語（專有名詞、引文、程式碼保留原文）。
- `[N2]` `Raw/` 檔名 `YYYY-MM-DD--slug.ext`，日期是**取得日**不是發表日。
- `[N3]` 一個檔名一個概念。同義詞寫進 `aliases`，不另開頁。
- `[N4]` 每頁都要有 frontmatter，欄位規範以 `Wiki/_templates/` 為準，`tools/lint.py` 強制檢查。
- `[N5]` **除了 `type: source`，每頁的 `sources` 都必須列出支撐它的來源頁。**

## 連結

- `[K1]` 一律 `[[wikilink]]`，要中文顯示用別名語法——但**別放進表格**，跳脫與否都會出事。
- `[K2]` 第一次提到有專頁的東西就連結，同一頁後續不必重複。
- `[K3]` 還沒有專頁的：值得成頁就開 `status: seed` 骨架頁，不值得就用粗體。**不留紅鏈。**
- `[K4]` 每頁至少 3 條出鏈、1 條入鏈，結尾固定放「## 相關頁面」，每條附一句關聯說明。

## 三種工作流程

`[W1]` 動作只有 `ingest` / `query` / `lint`，每一次都要追加一筆 `Wiki/log.md`。

- **ingest** `[W2]` —— 細節見 `.claude/skills/wiki-ingest/SKILL.md`。
  **預設一次只 ingest 一份**；先完整讀完，口頭回報 3–5 個重點等使用者確認，才動筆。
- **query** `[W3]` —— 先讀 `Wiki/index.md` 找候選頁，再讀內文，需要時才回頭讀 `Raw/` 原文。
  **回答必附 wikilink 引用**；wiki 裡沒有的明說「wiki 內查無」，
  要用模型知識或網路搜尋補洞必須先聲明。答完問一句要不要存進 wiki。
  `[W4]` 輸出格式依問題而定：比較題給表格、演變題給時間軸，要簡報就產 Marp、
  要圖表就產 matplotlib 圖存進 `Raw/assets/`。
- **lint** `[W5]` —— 跑 `python3 tools/lint.py` 拿機械性問題，再做判斷性檢查
  （見 `.claude/skills/wiki-lint/SKILL.md`）。輸出成清單逐項問，不要擅自大改。

## 寫作

`[S1]` 句子短，直述。**禁止空話**——「這是一個重要的概念」「有著深遠的影響」這類句子一律刪掉，
每一句都要帶資訊。
`[S2]` 數字、日期、人名、引文照抄來源，不改寫、不四捨五入、不憑記憶補。
`[S3]` 分不清是來源說的還是你推論的，標「（推論）」。
`[S4]` 頁面長度以夠用為準——entity 頁 100–300 字很正常，synthesis 頁可以到 1000 字以上。
`[S5]` 模板裡用不到的區塊直接刪掉，不要為湊格式寫空區塊。

## 禁止

1. `[L1]` 改 `Raw/`。
2. `[X2]` 虛構來源、引文、數字、URL。
3. `[X3]` 沒有來源支撐就寫進 wiki（純推論必須標「（推論）」）。
4. `[X4]` 用覆寫消滅矛盾——矛盾要並列雙方、各標來源與日期，並降低 `confidence`。
5. `[K3]` 留紅鏈。
6. `[W2]` 批次 ingest，除非使用者明說。
7. `[L3]` 改本檔規則，除非使用者同意。
